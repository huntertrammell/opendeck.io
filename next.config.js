/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["avatars.dicebear.com", "avatars.githubusercontent.com"],
  },
};

module.exports = nextConfig
